/**
 * Delay in ms before showing a loading indicator (spinner or skeleton) for
 * an in-flight request.  Avoids flicker when responses are fast or cached.
 */
export const SPINNER_DELAY_MS = 200

/**
 * How long the user must hold a button before the long-press action fires.
 * Used by useLongPress and any pointer-based interaction state machines
 * that implement long-press detection manually.
 */
export const LONG_PRESS_MS = 500
